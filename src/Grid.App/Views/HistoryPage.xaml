<Page
    x:Class="Grid.App.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer
        AutomationProperties.Name="Grid History"
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto">
        <Grid MaxWidth="1080" Padding="40,34,40,56" HorizontalAlignment="Center">
            <StackPanel Spacing="22">
                <StackPanel Spacing="7">
                    <TextBlock Text="History" FontSize="32" FontWeight="SemiBold" />
                    <TextBlock
                        Text="A record of real actions requested or completed through Grid. Internal diagnostics and assistant context bookkeeping are not shown here."
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        TextWrapping="Wrap" />
                </StackPanel>
                <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                    <ComboBox x:Name="ActorFilter" Header="ACTOR" SelectionChanged="OnFilterChanged" />
                    <ComboBox x:Name="StatusFilter" Grid.Column="1" Header="STATUS" SelectionChanged="OnFilterChanged" />
                </Grid>
                <InfoBar
                    x:Name="HistoryIssue"
                    AutomationProperties.LiveSetting="Assertive"
                    IsClosable="False"
                    IsOpen="False"
                    Severity="Warning"
                    Title="History unavailable" />
                <ListView x:Name="HistoryList" AutomationProperties.Name="User action history" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,10" Padding="18" Style="{StaticResource CardStyle}">
                                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="14">
                                    <StackPanel Spacing="4">
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding Detail}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding Context}" FontSize="11" Foreground="{ThemeResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" HorizontalAlignment="Right" Spacing="3">
                                        <TextBlock Text="{Binding Status}" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding Actor}" FontSize="11" />
                                        <TextBlock Text="{Binding When}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Border x:Name="EmptyState" Padding="24" Style="{StaticResource CardStyle}">
                    <StackPanel Spacing="7">
                        <TextBlock Text="No Grid actions have been recorded" FontSize="18" FontWeight="SemiBold" />
                        <TextBlock Text="Connections, refreshes, authorizations, and future approved actions will appear here only after they actually occur." Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
